apiVersion: v1
kind: Service
metadata:
  name: {{ include "evocloud-docs.fullname" . }}
  labels:
    {{- include "evocloud-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evocloud-docs.selectorLabels" . | nindent 4 }}
